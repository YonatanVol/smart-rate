<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Legacy Bluetooth permissions: only requested up to Android 11. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Android 12+: runtime permission, needed to read bonded devices and connect. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TapConnect">

        <!-- The one tap. Transparent, does its work, finishes. -->
        <activity
            android:name=".ConnectActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/shortcut_connect"
            android:launchMode="singleTop"
            android:theme="@style/Theme.TapConnect.Invisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".DevicePickerActivity"
            android:exported="true"
            android:label="@string/shortcut_pick"
            android:theme="@style/Theme.TapConnect" />

        <receiver
            android:name=".ConnectWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/connect_widget_info" />
        </receiver>
    </application>

</manifest>
